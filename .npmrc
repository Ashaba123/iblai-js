# Force pnpm to fetch these packages from npm registry instead of workspace
# This is needed because these packages exist in a sibling workspace (ibl-frontend)
link-workspace-packages=false

# Alternatively, you can use:
# public-hoist-pattern[]=@iblai/data-layer
# public-hoist-pattern[]=@iblai/web-utils
# public-hoist-pattern[]=@iblai/web-containers
