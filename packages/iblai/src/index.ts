/**
 * @iblai/iblai - Unified package for IBL AI
 *
 * This package re-exports all exports from:
 * - @iblai/data-layer: API slices, hooks, and data management
 * - @iblai/web-utils: Utilities, providers, and helper functions
 * - @iblai/web-containers: UI components and containers
 *
 * Usage:
 * import { useGetMfeContextQuery, AuthProvider, Button } from '@iblai/iblai';
 */

// Re-export everything from data-layer
export * from '@iblai/data-layer';

// Re-export everything from web-utils
export * from '@iblai/web-utils';

// Re-export everything from web-containers
export * from '@iblai/web-containers';
